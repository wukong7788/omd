# SEC retained SGML and declared-package unit repair v2

- Goal: extend the [observed unit repair](sec-compound-unit-repair.md) to new offline SGML and declared-availability package productions, preserving explicit v1 outputs and all historical replay; live-provider and standalone statement parsing remain a separately documented follow-up.
- Files: `sgml_financials.py`, `xbrl_package_financials.py`, `_pit_query.py`, focused shared private unit application if useful, tests and linked documentation/changelog. Reuse the reviewed raw-unit decoder unchanged. Keep `_rows_from_documents`, shared native parser, observed v1/v2 semantics, provider client, dependency pin, schemas and unrelated edits unchanged.
- SGML API: add keyword `parser_version` to `produce_sec_financials_from_sgml`, default `sec-sgml-financial-parser-v2-edgartools-5.56.0`; accept exactly that or `sec-sgml-financial-parser-v1-edgartools-5.56.0`. Select corresponding `sec-sgml-financial-adapter-v2` or existing `sec-sgml-financial-adapter-v1`; reject unknown/type-invalid versions before replay or writes.
- Package API: add keyword `parser_version` to `produce_sec_financials_from_xbrl_package`, default `sec-xbrl-package-financial-parser-v2-edgartools-5.56.0`; accept exactly that or `sec-xbrl-package-financial-parser-v1-edgartools-5.56.0`. Select corresponding `sec-xbrl-package-financial-adapter-v2` or existing `sec-xbrl-package-financial-adapter-v1`; do not accept another producer family's version.
- Result/configuration: expose the selected `parser_version` on each returned production as a keyword-only field with historical v1 default, preserving existing positional construction. V1 retains its exact configuration hash formula; v2 uses the same formula with its selected parser version. Keep normalization version, normalized schema and typed-row projection serialization unchanged; adapter/configuration identities distinguish v2 normalized versions even when simple-unit projection bytes and vintage identity remain equal.
- Build: v1 executes its original full parsing path without raw-unit correction. V2 obtains the actual retained instance from validated SGML documents or the replayed bound package, validates its complete raw unit table before native parsing, calls the unchanged native row path, then replaces only each emitted row's `unit` through its exact `unit_ref` before vintage/projection creation. Missing referenced units or malformed declarations, including unused units, fail before any output observation; no upstream-unit fallback, mutation, guessed unit names or scaling.
- Unit contract: reuse exact namespace/QName, direct measure/product/divide, canonical JSON, multiplicity and currency semantics from the observed repair. Standard simple units stay equal; compound currency is None. Preserve all other row facts, native values, context/dimensions/periods, precision, statement selection/order and explicit empty/coverage behavior.
- Availability invariant: both automatic SGML adapter versions are acceptance proxies. `MARKET_KNOWN` must explicitly reject either v1 or v2 before policy/version/cutoff/quality filtering, including retained inputs; v2 unit correction never establishes public availability. `SYSTEM_REPLAY` keeps existing quality/commit/cutoff behavior. Declared-package acceptance/source-availability validation remains unchanged and independent of unit versions.
- Persistence: never overwrite old projections, snapshots, observations or bundles. Existing PIT bundle replay continues reconstructing normalized versions from retained projections with exact stored adapter/configuration identities; it must not rerun a new default parser or relabel v1 as v2. To reproduce source-derived historical output, callers explicitly select v1; all v2 quality records and consumer commits bind their own normalized versions.
- Bounds/errors: retain current raw/component/XML/row/output caps and stricter-only caller limits; raw-unit decoding uses existing bounded validated instance bytes and its fixed decoder limits. Version/unit/schema/coverage/availability errors complete before the sole output observation; no partial-success result, extra fetch, new parser deadline, public-time promotion or operational permission.
- Acceptance: pre-change synthetic v1 golden projection bytes, observation fact identities, vintage/content/normalized-version identities and legacy bundles remain exact for both producers; observed v1/v2 regressions remain green. Default v2 and explicit v1 differ only in intended unit/derived currency and version identities; include real-parser divide/product/simple-unit cases and assert every other row field unchanged.
- Acceptance continued: for each producer test invalid/cross-family version, malformed-unused/duplicate/missing-reference units with zero output writes, explicit v1 retention, v2 adapter/configuration binding and mixed v1/v2 bundle/query quality/commit isolation. Test SGML v1/v2 MARKET_KNOWN rejection despite irrelevant selectors/future cutoff and SYSTEM_REPLAY preservation; retain declared-package availability failure regressions. Reuse prior decoder grammar/security/limit tests, with direct producer boundary evidence for new integration.
- Evidence/scope: a retained observed-v2 real-filing result may corroborate the shared unit representation, but does not count as a successful SGML build when no embedded instance exists or as declared-package evidence when publication timing is absent. Synthetic real-parser qualification is sufficient for this bounded code slice; retain those real-source gaps honestly and grant no financial PASS or consumer parity without evidence.
- Stop/next: stop for changed availability semantics, required projection schema changes or failure to preserve v1 identities. No live client change, blanket live disable, network, wider sample, publication, or consumer edit. Document default-v2 migration and assess affected consumer reruns before corrected downstream comparisons; then design the separate raw-backed live-provider unit boundary. Independent actual-diff review and current targeted checks are required.
